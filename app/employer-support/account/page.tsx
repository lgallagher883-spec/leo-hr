import { redirect } from "next/navigation"; import { getEmployerSupportAccess } from "@/lib/auth/employerSupportAccess"; import EmployerSupportShell from "../EmployerSupportShell"; import styles from "../employer-support-portal.module.css"; export default async function AccountPage(){const access=await getEmployerSupportAccess();if(!access)redirect("/employer-support/sign-in");return <EmployerSupportShell><main className={styles.content}><div className={styles.narrow}><p className={styles.eyebrow}>My Account</p><h1>Employer Account</h1><section className={styles.notice}><strong>Your account is separate from individual Matters.</strong><p>Closing an individual Matter will not close this employer account or affect another Matter. Account closure controls will be added with the retention and deletion workflow.</p></section></div></main></EmployerSupportShell>}
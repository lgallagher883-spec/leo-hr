import { redirect } from "next/navigation"; import { getEmployerSupportAccess } from "@/lib/auth/employerSupportAccess"; import EmployerSupportShell from "../EmployerSupportShell"; import styles from "../employer-support-portal.module.css"; export default async function HelpPage(){const access=await getEmployerSupportAccess();if(!access)redirect("/employer-support/sign-in");return <EmployerSupportShell><main className={styles.content}><div className={styles.narrow}><p className={styles.eyebrow}>Help & Support</p><h1>Help With Employer Support</h1><section className={styles.notice}><strong>Ask Leo leads the HR process inside each Matter.</strong><p>For account or technical support, use Leo HR support. Sensitive employee details should stay inside the relevant Matter rather than being sent in ordinary support messages.</p></section></div></main></EmployerSupportShell>}